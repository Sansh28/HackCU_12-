# Savant Backend Package

This package contains the backend application code split into:

- `main.py`: FastAPI app assembly
- `routers/`: HTTP route handlers
- `services/`: orchestration and external-service calls
- `config.py`, `models.py`, `security.py`, `store.py`: shared backend modules
