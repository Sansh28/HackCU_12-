# Legacy Frontend Compatibility Path

This directory exists only as a compatibility wrapper for external services
that may still reference the old `savant-frontend` project root.

The actual frontend application now lives in `apps/frontend`.
